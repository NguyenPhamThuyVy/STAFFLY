<UserControl x:Class="StafflyApp.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             Background="Transparent" FontFamily="Segoe UI">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Analytics Overview" FontSize="28" FontWeight="Bold" Foreground="#1A1C29"/>
                <TextBlock Text="Real-time workforce and payroll insights" Foreground="#6B7280" FontSize="14"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Border Background="White" CornerRadius="15" Padding="20,15" Margin="0,0,15,0" Width="180">
                    <StackPanel>
                        <TextBlock Text="Total Staff" FontSize="12" Foreground="#6B7280"/>
                        <TextBlock Text="{Binding TotalEmployees}" FontSize="24" FontWeight="Bold" Foreground="#1954D1"/>
                    </StackPanel>
                </Border>
                <Border Background="White" CornerRadius="15" Padding="20,15" Width="220">
                    <StackPanel>
                        <TextBlock Text="Total Payroll (Monthly)" FontSize="12" Foreground="#6B7280"/>
                        <TextBlock Text="{Binding TotalPayroll, StringFormat='{}{0:C0}'}" FontSize="24" FontWeight="Bold" Foreground="#1A365D"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6.5*"/>
                <ColumnDefinition Width="3.5*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="20" Padding="25" Margin="0,0,15,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                        <materialDesign:PackIcon Kind="TrendingUp" Foreground="#1954D1" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="Workforce Growth Trends" FontSize="18" FontWeight="SemiBold" Foreground="#1A1C29"/>
                    </StackPanel>

                    <lvc:CartesianChart Grid.Row="1" Series="{Binding StaffGrowthSeries}" LegendLocation="None">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="Months" Labels="{Binding MonthsLabels}" Separator="{x:Static lvc:DefaultAxes.CleanSeparator}"/>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="Headcount" MinValue="0"/>
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </Grid>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="20" Padding="25">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Payroll by Dept" FontSize="18" FontWeight="SemiBold" Foreground="#1A1C29" Margin="0,0,0,20"/>

                    <lvc:PieChart Grid.Row="1" Series="{Binding SalaryPieSeries}" LegendLocation="Bottom" InnerRadius="40">
                        <lvc:PieChart.ChartLegend>
                            <lvc:DefaultLegend BulletSize="10" Foreground="#6B7280"/>
                        </lvc:PieChart.ChartLegend>
                    </lvc:PieChart>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="#1A365D" CornerRadius="20" Padding="20" Margin="0,20,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="InfoOutline" Foreground="#A0B9DF" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="System Suggestion: 3 Departments are approaching their headcount limits." 
                               Foreground="White" VerticalAlignment="Center" FontSize="14"/>
                </StackPanel>
                <Button Grid.Column="1" Content="View Detailed Report" 
                        Style="{StaticResource MaterialDesignFlatButton}"
                        Foreground="#A0B9DF" FontWeight="Bold"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>