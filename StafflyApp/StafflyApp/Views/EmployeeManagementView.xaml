<UserControl x:Class="StafflyApp.Views.EmployeeManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="Transparent">

    <materialDesign:DialogHost IsOpen="{Binding IsDialogOpen}" Identifier="MainDialog">
        <materialDesign:DialogHost.DialogContent>
            <Border Padding="30" Width="400" Background="White" CornerRadius="20">
                <StackPanel>
                    <TextBlock Text="{Binding FormTitle}" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" Foreground="#1954D1"/>
                    <TextBox materialDesign:HintAssist.Hint="Họ tên" Text="{Binding EditingEmployee.FullName}" Margin="0,10" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    <TextBox materialDesign:HintAssist.Hint="Email" Text="{Binding EditingEmployee.Email}" Margin="0,10" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    <TextBox materialDesign:HintAssist.Hint="Số điện thoại" Text="{Binding EditingEmployee.Phone}" Margin="0,10" Style="{StaticResource MaterialDesignOutlinedTextBox}"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                        <Button Content="HỦY" Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}" Style="{StaticResource MaterialDesignFlatButton}"/>
                        <Button Content="LƯU" Command="{Binding SaveEmployeeCommand}" Margin="10,0,0,0" Background="#1954D1" Foreground="White"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </materialDesign:DialogHost.DialogContent>

        <Border Background="White" CornerRadius="20" Padding="25" Margin="10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Company Employees" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

                <Grid Grid.Row="1" Margin="0,0,0,20">
                    <TextBox Width="300" HorizontalAlignment="Left" materialDesign:HintAssist.Hint="Search..." Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                    <Button HorizontalAlignment="Right" Content="+ Add Employee" Command="{Binding OpenAddDialogCommand}" Background="#1954D1" Foreground="White" BorderThickness="0"/>
                </Grid>

                <ScrollViewer Grid.Row="2">
                    <ItemsControl ItemsSource="{Binding Employees}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#F3F4F6" BorderThickness="0,0,0,1" Padding="10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="60"/>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="100"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding EmployeeID}"/>
                                        <TextBlock Grid.Column="1" Text="{Binding FullName}" Foreground="#1954D1" FontWeight="Bold"/>
                                        <TextBlock Grid.Column="2" Text="{Binding Email}"/>
                                        <StackPanel Grid.Column="3" Orientation="Horizontal">
                                            <Button Style="{StaticResource MaterialDesignIconButton}" Command="{Binding DataContext.OpenEditDialogCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}">
                                                <materialDesign:PackIcon Kind="Pencil" Foreground="Gray"/>
                                            </Button>
                                            <Button Style="{StaticResource MaterialDesignIconButton}" Command="{Binding DataContext.DeleteEmployeeCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}">
                                                <materialDesign:PackIcon Kind="Delete" Foreground="Red"/>
                                            </Button>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>
    </materialDesign:DialogHost>
</UserControl>