<UserControl x:Class="StafflyApp.Views.EmployeeManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:StafflyApp.Views"
             xmlns:viewmodels="clr-namespace:StafflyApp.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1050"
             Background="Transparent" FontFamily="Segoe UI">

  

    <Border Background="White" CornerRadius="12" Padding="30" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Color="#DDDDDD" ShadowDepth="2" Opacity="0.5"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="15"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <TextBlock Text="COMPANY EMPLOYEES" FontSize="15" FontWeight="Bold" Foreground="#333333" VerticalAlignment="Center" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Employees" Style="{StaticResource MaterialDesignOutlinedButton}" BorderBrush="#E0E0E0" Foreground="#FF2945FF" FontWeight="Bold" Height="36" materialDesign:ButtonAssist.CornerRadius="4,0,0,4"/>
                    <Button Content="Live View" Style="{StaticResource MaterialDesignOutlinedButton}" BorderBrush="#E0E0E0" Foreground="#757575" Height="36" materialDesign:ButtonAssist.CornerRadius="0"/>
                    <Button Content="Org Chart" Style="{StaticResource MaterialDesignOutlinedButton}" BorderBrush="#E0E0E0" Foreground="#757575" Height="36" materialDesign:ButtonAssist.CornerRadius="0,4,4,0"/>
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" BorderBrush="#F0F0F0" BorderThickness="0,0,0,1" Margin="0,15,0,14"/>

            <Grid Grid.Row="2">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <TextBox Style="{StaticResource MaterialDesignOutlinedTextBox}" 
                             Width="250" Height="40" 
                             materialDesign:HintAssist.Hint="Search Employee..." 
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Padding="10,0" Margin="0,0,15,0">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>

                    <ComboBox Style="{StaticResource MaterialDesignOutlinedComboBox}" 
                              Width="150" Height="40" 
                              ItemsSource="{Binding Departments}"
                              DisplayMemberPath="DepartmentName"
                              SelectedIndex="0" Padding="10,0">
                    </ComboBox>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource MaterialDesignOutlinedButton}" Foreground="#757575" Margin="0,0,15,0"/>
                    <Button Content="Add Employees" Command="{Binding AddNewEmployeeCommand}" Background="#FF2945FF" Foreground="White" BorderThickness="0" Height="40" materialDesign:ButtonAssist.CornerRadius="4"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="4" Margin="10,0,20,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="280"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="60"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="ID" FontWeight="Bold" Foreground="#1A1A1A"/>
                <TextBlock Grid.Column="1" Text="Name" FontWeight="Bold" Foreground="#1A1A1A"/>
                <TextBlock Grid.Column="2" Text="Dept ID" FontWeight="Bold" Foreground="#1A1A1A"/>
                <TextBlock Grid.Column="3" Text="Email" FontWeight="Bold" Foreground="#1A1A1A"/>
                <TextBlock Grid.Column="4" Text="Hire Date" FontWeight="Bold" Foreground="#1A1A1A"/>
                <TextBlock Grid.Column="5" Text="Action" FontWeight="Bold" Foreground="#1A1A1A" HorizontalAlignment="Center"/>
            </Grid>

            <ScrollViewer Grid.Row="6" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Employees}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#F5F5F5" BorderThickness="0,0,0,1" Padding="10,15,20,15">
                                <Grid VerticalAlignment="Center">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="280"/>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="250"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="60"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="{Binding EmployeeID}" VerticalAlignment="Center" FontWeight="SemiBold"/>

                                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                                        <Border Width="35" Height="35" CornerRadius="17.5" Background="#E8EAF6" Margin="0,0,12,0">
                                            <TextBlock Text="EMP" FontSize="10" Foreground="#624BFF" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                                        </Border>
                                        <TextBlock Text="{Binding FullName}" VerticalAlignment="Center" Foreground="#624BFF" FontWeight="SemiBold"/>
                                    </StackPanel>

                                    <TextBlock Grid.Column="2" Text="{Binding DepartmentID}" VerticalAlignment="Center" Foreground="#555"/>

                                    <TextBlock Grid.Column="3" Text="{Binding Email}" VerticalAlignment="Center" Foreground="#333"/>

                                    <TextBlock Grid.Column="4" Text="{Binding DateOfBirth, StringFormat='{}{0:dd/MM/yyyy}'}" VerticalAlignment="Center"/>

                                    <Button Grid.Column="5" Content="DEL" FontSize="10" Padding="0" Foreground="Red" 
                                            Style="{StaticResource MaterialDesignFlatButton}"
                                            Command="{Binding DataContext.DeleteSelectedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>