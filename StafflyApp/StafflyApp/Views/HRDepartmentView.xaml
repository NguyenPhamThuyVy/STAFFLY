<UserControl x:Class="StafflyApp.Views.HRDepartmentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             Background="#F4F6F9" FontFamily="Segoe UI">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="DEPARTMENT ANALYTICS &amp; CAPACITY AUDIT" FontSize="18" FontWeight="Bold" Foreground="#1A365D"/>
            <TextBlock Text="Monitor compliance thresholds vs headcount limits and check attendance health indexes." Foreground="#666"/>
        </StackPanel>

        <UniformGrid Grid.Row="1" Columns="2">
            <Border Background="White" CornerRadius="16" Padding="15" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="👥 Headcount Capacity: Actual vs Limit Boundary" FontSize="13" FontWeight="Bold" Foreground="#333" Margin="0,0,0,15"/>
                    <lvc:CartesianChart Grid.Row="1" Series="{Binding CapacityComparisonSeries}" LegendLocation="Top">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="Departments" Labels="{Binding DeptLabels}"/>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="Headcount (Persons)" MinValue="0"/>
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </Grid>
            </Border>

            <Border Background="White" CornerRadius="16" Padding="15" Margin="10,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="⚠️ Attendance Exceptions: Absence (Column) vs Tardy Lateness (Line)" FontSize="13" FontWeight="Bold" Foreground="#BE123C" Margin="0,0,0,15"/>
                    <lvc:CartesianChart Grid.Row="1" Series="{Binding AttendanceExceptionsSeries}" LegendLocation="Top">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="Timeline Period" Labels="{Binding TimelineLabels}"/>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="Count (Times)"/>
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </Grid>
            </Border>
        </UniformGrid>
    </Grid>
</UserControl>