<UserControl x:Class="StafflyApp.Views.PayrollView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="Transparent">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Border Background="White" CornerRadius="20" Padding="25" Margin="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Import Payroll Data" FontSize="24" FontWeight="Bold" Foreground="#1954D1" Margin="0,0,0,20"/>

            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="SELECT EXCEL FILE" Command="{Binding ImportExcelCommand}" 
                        Background="#1954D1" Foreground="White" BorderThickness="0" 
                        materialDesign:ButtonAssist.CornerRadius="10">
                    <Button.Effect>
                        <DropShadowEffect BlurRadius="10" Opacity="0.2" Color="#1954D1"/>
                    </Button.Effect>
                </Button>

                <TextBlock Grid.Column="1" Text="{Binding FilePath}" VerticalAlignment="Center" Margin="15,0,0,0" Foreground="#757575" FontStyle="Italic"/>

                <Button Grid.Column="2" Content="SAVE DATA" Command="{Binding SaveToDatabaseCommand}" 
                        Visibility="{Binding IsDataLoaded, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Background="#4CAF50" Foreground="White" BorderThickness="0" 
                        materialDesign:ButtonAssist.CornerRadius="10" >
                    <Button.Effect>
                        <DropShadowEffect BlurRadius="10" Opacity="0.2" Color="#4CAF50"/>
                    </Button.Effect>
                </Button>
            </Grid>

            <DataGrid Grid.Row="2" ItemsSource="{Binding ImportedRecords}" AutoGenerateColumns="False" 
                      CanUserAddRows="False" IsReadOnly="True" Style="{StaticResource MaterialDesignDataGrid}"
                      BorderBrush="#E0E0E0" BorderThickness="1" HeadersVisibility="Column" Background="White">

                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow" BasedOn="{StaticResource MaterialDesignDataGridRow}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsValid}" Value="False">
                                <Setter Property="Background" Value="#FFEbee"/>
                                <Setter Property="Foreground" Value="#D32F2F"/>
                                <Setter Property="ToolTip" Value="{Binding ErrorNote}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Emp ID" Binding="{Binding EmployeeID}" Width="80"/>

                    <DataGridTextColumn Header="Full Name" Binding="{Binding EmployeeName}" Width="160">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTemplateColumn Header="Month/Year" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Month, StringFormat={}{0:D2}}"/>
                                    <TextBlock Text="/"/>
                                    <TextBlock Text="{Binding Year}"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTextColumn Header="Total Salary" Binding="{Binding TotalSalary, StringFormat={}{0:N0} USD}" Width="150"/>

                    <DataGridTextColumn Header="Bonus" Binding="{Binding TotalBonus, StringFormat={}{0:N0} USD}" Width="120"/>

                    <DataGridTextColumn Header="Error Details" Binding="{Binding ErrorNote}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,15,0,0">
                <materialDesign:PackIcon Kind="InformationOutline" Foreground="#757575" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBlock Foreground="#757575" FontSize="13" VerticalAlignment="Center"
                           Text="Rows highlighted in red contain invalid data and will be automatically excluded during the saving process."/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>