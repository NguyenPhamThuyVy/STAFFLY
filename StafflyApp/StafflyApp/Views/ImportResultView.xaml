<UserControl x:Class="StafflyApp.Views.ImportResultView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="White" Width="550" Height="500">

    <Border CornerRadius="20" Padding="25">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                <materialDesign:PackIcon Kind="FileImport" Foreground="#1954D1" Width="30" Height="30" VerticalAlignment="Center"/>
                <TextBlock Text="Import Result Summary" FontSize="22" FontWeight="Bold" Foreground="#1A1C29" Margin="15,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <UniformGrid Grid.Row="1" Columns="2" Margin="0,0,0,20">
                <materialDesign:Card Background="#E8F5E9" UniformCornerRadius="12" Padding="15" Margin="0,0,10,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="CheckCircle" Foreground="#2E7D32" Width="24" Height="24" VerticalAlignment="Center"/>
                        <StackPanel Margin="15,0,0,0">
                            <TextBlock Text="Successful" FontSize="12" Foreground="#2E7D32" FontWeight="Medium"/>
                            <TextBlock Text="{Binding SuccessCount}" FontSize="24" FontWeight="Bold" Foreground="#2E7D32"/>
                        </StackPanel>
                    </StackPanel>
                </materialDesign:Card>

                <materialDesign:Card Background="#FFEBEE" UniformCornerRadius="12" Padding="15" Margin="10,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="AlertCircle" Foreground="#C62828" Width="24" Height="24" VerticalAlignment="Center"/>
                        <StackPanel Margin="15,0,0,0">
                            <TextBlock Text="Failed" FontSize="12" Foreground="#C62828" FontWeight="Medium"/>
                            <TextBlock Text="{Binding FailureCount}" FontSize="24" FontWeight="Bold" Foreground="#C62828"/>
                        </StackPanel>
                    </StackPanel>
                </materialDesign:Card>
            </UniformGrid>

            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Detailed Error Report:" FontSize="14" FontWeight="SemiBold" Foreground="#6B7280" Margin="0,0,0,10"/>

                <Border Grid.Row="1" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8">
                    <DataGrid ItemsSource="{Binding ErrorList}" 
                              AutoGenerateColumns="False" 
                              IsReadOnly="True" 
                              Background="Transparent"
                              BorderThickness="0"
                              CanUserAddRows="False"
                              VerticalScrollBarVisibility="Auto">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Row" Binding="{Binding RowNumber}" Width="60" FontWeight="Bold"/>
                            <DataGridTextColumn Header="Subject" Binding="{Binding EmployeeName}" Width="150"/>
                            <DataGridTextColumn Header="Description" Binding="{Binding ErrorDetail}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextWrapping" Value="Wrap"/>
                                        <Setter Property="Foreground" Value="#C62828"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
            </Grid>

            <Button Grid.Row="3" 
                    Content="DONE" 
                    Margin="0,20,0,0" 
                    Height="45" 
                    materialDesign:ButtonAssist.CornerRadius="10"
                    Command="{x:Static materialDesign:DialogHost.CloseDialogCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"/>
        </Grid>
    </Border>
</UserControl>