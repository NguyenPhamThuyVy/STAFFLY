<UserControl x:Class="StafflyApp.Views.EmployeeProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="Transparent" FontFamily="Segoe UI">

    <Border Background="White" CornerRadius="25" Padding="40" Margin="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,30">
                <Button HorizontalAlignment="Left" Style="{StaticResource MaterialDesignFlatButton}" 
                        Command="{Binding GoBackCommand}" Foreground="#6B7280" Padding="0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="ArrowLeft" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBlock Text="Back to Employee List" VerticalAlignment="Center" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>
            </Grid>

            <Grid Grid.Row="1" Margin="0,0,0,40">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Width="120" Height="120" CornerRadius="60" Background="#F3F4F6" BorderBrush="#E5E7EB" BorderThickness="2" Margin="0,0,30,0">
                    <materialDesign:PackIcon Kind="AccountTie" Width="80" Height="80" Foreground="#9CA3AF" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="{Binding SelectedEmployee.FullName}" FontSize="32" FontWeight="ExtraBold" Foreground="#1A1C29" Margin="0,0,0,5"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <Border Background="#E0E7FF" CornerRadius="6" Padding="10,4" Margin="0,0,10,0">
                            <TextBlock Text="{Binding SelectedEmployee.DepartmentName}" Foreground="#1954D1" FontWeight="Bold" FontSize="13"/>
                        </Border>
                        <Border Background="#ECFDF5" CornerRadius="6" Padding="10,4">
                            <TextBlock Text="{Binding SelectedEmployee.Status}" Foreground="#10B981" FontWeight="Bold" FontSize="13"/>
                        </Border>
                    </StackPanel>
                    <TextBlock Text="{Binding SelectedEmployee.EmployeeID, StringFormat='Employee ID: {0}'}" Foreground="#6B7280" FontSize="14" FontWeight="Medium"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,20,0">
                    <TextBlock Text="PERSONAL DETAILS" FontSize="14" FontWeight="Bold" Foreground="#9CA3AF" Margin="0,0,0,20"/>

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Kind="PhoneOutline" Foreground="#6B7280" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Phone Number" FontSize="12" Foreground="#9CA3AF"/>
                            <TextBlock Text="{Binding SelectedEmployee.Phone}" FontSize="15" FontWeight="SemiBold" Foreground="#1A1C29"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Kind="EmailOutline" Foreground="#6B7280" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Email Address" FontSize="12" Foreground="#9CA3AF"/>
                            <TextBlock Text="{Binding SelectedEmployee.Email}" FontSize="15" FontWeight="SemiBold" Foreground="#1A1C29"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <materialDesign:PackIcon Kind="CakeVariantOutline" Foreground="#6B7280" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Date of Birth" FontSize="12" Foreground="#9CA3AF"/>
                            <TextBlock Text="{Binding SelectedEmployee.DateOfBirth, StringFormat={}{0:dd/MM/yyyy}}" FontSize="15" FontWeight="SemiBold" Foreground="#1A1C29"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>

                <Border Grid.Column="1" Background="#F9FAFB" CornerRadius="16" Padding="25">
                    <StackPanel>
                        <TextBlock Text="DEPARTMENT TRANSFER" FontSize="14" FontWeight="Bold" Foreground="#1954D1" Margin="0,0,0,20"/>
                        <TextBlock Text="Select a new destination department for this employee. This action will update their primary operational unit." TextWrapping="Wrap" Foreground="#6B7280" Margin="0,0,0,20"/>

                        <ComboBox materialDesign:HintAssist.Hint="Target Department" 
                                  ItemsSource="{Binding Departments}" 
                                  DisplayMemberPath="DepartmentName"
                                  SelectedItem="{Binding SelectedTargetDept}"
                                  Style="{StaticResource MaterialDesignOutlinedComboBox}" Margin="0,0,0,20" Background="White"/>

                        <Button Content="Execute Transfer" Command="{Binding ConfirmTransferCommand}" Background="#1954D1" Foreground="White" BorderThickness="0" Height="45" materialDesign:ButtonAssist.CornerRadius="8" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Border>
</UserControl>